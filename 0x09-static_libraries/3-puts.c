#include "main.h"
#include <stdio.h>
/**
 * _puts - print string and then newline
 * Descreption: print string and then newline
 * @str: your typical string
 * Return: 0 (Success)
 */
void _puts(char *str)
{
	puts(str);
}
